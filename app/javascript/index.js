import React from 'react';
import ReactDOM from 'react-dom';
import KartButton from './components/kart_button';

document.addEventListener('DOMContentLoaded', () => {
  ReactDOM.render(<KartButton />, document.getElementById('root-component'))
})
